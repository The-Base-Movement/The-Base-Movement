# Codex Skills Index

Global forbidden assumptions: no Figma assumption, no Tailwind conversion or new Tailwind usage, no shadcn/ui, no Lucide icons, and no new UI/icon libraries unless explicitly requested.

| Skill                           | What it is for                                                                     | Inspect first                                                          | Docs                                                             | Validation                                                    |
| ------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------- |
| `custom-ui`                     | Custom dashboard/admin UI styling                                                  | `src/index.css`, target page/component, layout components              | `docs/audits/layout_guidelines.md`, design-system handoff        | `npm run typecheck`, `npm run build` if cross-surface         |
| `kpi-dashboard`                 | KPI cards, charts, tables, dashboard metrics                                       | target `*KPIs.tsx`, `TacticalKPI`, service feeding metrics             | `docs/audits/kpi-stats-reference.md`                             | `npm run typecheck`, `npm run test:run` if logic              |
| `bugfix`                        | Small behavior or compile fixes                                                    | failing file, nearest service/helper/test                              | relevant docs only                                               | `npm run typecheck`, relevant tests                           |
| `forms`                         | Registration, settings, modals, inputs                                             | target form, related service/type                                      | form or feature docs                                             | `npm run typecheck`, `npm run test:run` if covered            |
| `auth`                          | Login, session, route guards, verification                                         | `src/lib/supabase.ts`, `AuthContext`, guards, auth service             | `docs/database/users-column-security.md` when user data involved | `npm run typecheck`, `npm run test:run`                       |
| `database`                      | Migrations, RLS, RPCs, Supabase schema                                             | `supabase/migrations`, target service/type                             | `docs/database/users-column-security.md`, database docs          | `npm run typecheck`; DB commands not available as npm scripts |
| `api-routes`                    | Supabase edge functions and service calls                                          | `supabase/functions`, `src/services`, `.env.example`                   | API and feature docs                                             | `npm run typecheck`, `npm run test:run`                       |
| `seo`                           | Public routes, metadata, sitemap, prerender                                        | target public page, `routes.tsx`, `vite.config.ts`, `public` SEO files | `docs/homepage.md`, project docs                                 | `npm run build`                                               |
| `email-templates`               | Transactional/newsletter templates                                                 | `_shared/email-templates.ts`, calling function/service                 | email audits and Resend/SendGrid docs                            | `npm run typecheck`                                           |
| `deployment`                    | Vercel/Vite build and runtime issues                                               | `vercel.json`, `vite.config.ts`, `package.json`, `.env.example`        | deployment and operations docs                                   | `npm run build`                                               |
| `docs-architecture`             | Docs-led architecture or business-rule tasks                                       | relevant `docs/` file, then target code                                | `docs/` by topic                                                 | relevant command only                                         |
| `admin-panel`                   | Admin command center pages                                                         | `src/pages/admin`, `AdminLayout`, `AdminPageHeader`, services          | admin/mobile/layout docs                                         | `npm run typecheck`                                           |
| `role-permissions`              | Admin roles, permissions, gated operations                                         | `adminService`, `roleService`, `types/admin.ts`, admin settings        | database/security docs                                           | `npm run typecheck`, tests if covered                         |
| `reports`                       | Analytics/reporting/export views                                                   | report page, service, chart/KPI component                              | relevant plan/spec/audit docs                                    | `npm run typecheck`, `npm run test:run`                       |
| `performance`                   | Bundle, render, query, image, prerender cleanup                                    | slow component/service, hooks, `vite.config.ts`                        | performance/image/mobile docs                                    | `npm run typecheck`, `npm run build` if bundle                |
| `accessibility`                 | Keyboard, ARIA, focus, semantic UI                                                 | target component/page and state components                             | layout/design docs                                               | `npm run typecheck`                                           |
| `testing`                       | Vitest and Testing Library work                                                    | `src/test`, target module, existing tests                              | feature docs if behavior-driven                                  | `npm run test:run`                                            |
| `data-import-export`            | CSV/PDF/import/export/scanning flows                                               | target import/export page, scripts, services                           | CSV, KYC, scan, operational docs                                 | `npm run typecheck`, `npm run test:run`                       |
| `nextjs-data-exposure-guard`    | Next.js data exposure audits and analogous client leak checks                      | Next.js app files, env files, API routes, client props                 | not available; use when a Next.js project/migration is involved  | Next.js build or repo validation as applicable                |
| `political-movement-supervisor` | Project-wide political movement security, content, compliance, and launch judgment | target flow, `AGENTS.md`, relevant project skill/docs                  | `docs/audits/`, `docs/project-docs/`, `docs/database/`           | relevant command from `AGENTS.md`                             |
