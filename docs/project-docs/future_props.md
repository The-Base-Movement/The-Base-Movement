# Future Props & Interfaces

This document preserves props and interfaces that have been defined but are currently unused in the mock implementation to avoid linting errors.

## CommentSection.tsx
```typescript
interface CommentSectionProps {
  postId: string
}
```

## ReviewSection.tsx
```typescript
interface ReviewSectionProps {
  productId: string | number
}
```

## MemberProfileCard.tsx (If applicable)
*Currently using all props*

## ChapterCard.tsx (If applicable)
*Currently using all props*
