/**
 * blogs/constants.ts
 * ─────────────────────────────────────────────────────────────────
 * Shared constants used across Blogs sub-components.
 * DEFAULT_PLACEHOLDER: single branded cover image for posts without a featured image.
 */

/** Fallback cover image when a post has no imageUrl */
export const DEFAULT_PLACEHOLDER = '/branding/og-image.png'

/** Allowed blog post categories */
export const CATEGORIES = ['Movement', 'Youth', 'Economy', 'Diaspora', 'Integrity', 'Community']
