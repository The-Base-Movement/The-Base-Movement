/**
 * Youth Wing visual identity. Reuses the site's design-system tokens but swaps
 * the brand green for a distinct teal so the youth track never reads as the
 * adult Ghana Resident / Diaspora party tracks. The variables themselves live
 * in src/index.css under `.yw-scope` (they need a dark-mode variant, which an
 * inline style object cannot express).
 */
export const YW_SCOPE = 'yw-scope'
export const YW_ACCENT = 'hsl(var(--yw-accent))'
export const YW_ACCENT_SOFT = 'hsl(var(--yw-accent-soft))'
